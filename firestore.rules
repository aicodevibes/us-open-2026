rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // ===============================================================
    // Assumed Data Model (The Open Events prefix)
    // ===============================================================

    // Helper Functions
    function isAuthenticated() {
      return request.auth != null;
    }

    function isAdmin() {
      return isAuthenticated() && 
        (request.auth.token.email_verified == true) &&
        exists(/databases/$(database)/documents/theopen_users/$(request.auth.uid)) && 
        get(/databases/$(database)/documents/theopen_users/$(request.auth.uid)).data.role == 'admin';
    }

    // Golf Events & Subcollections: Anyone can read, only admin can write
    match /theopen_events/{eventId} {
      allow read: if true;
      allow write: if isAdmin();

      match /participants/{participantId} {
        allow read: if true;
        allow write: if isAdmin();
      }

      match /greedyParticipants/{participantId} {
        allow read: if true;
        allow write: if isAdmin();
      }

      match /playerScores/{playerName} {
        allow read: if true;
        allow write: if isAdmin();
      }

      match /playoffScores/{playerName} {
        allow read: if true;
        allow write: if isAdmin();
      }
    }

    // Golf Config: Anyone can read, only admin can write
    match /theopen_config/{configId} {
      allow read: if true;
      allow write: if isAdmin();
    }

    // ===============================================================
    // Legacy Rules (for fallback / compatibility - updated to theopen_)
    // ===============================================================

    // Participants: Anyone can read, only admin can write
    match /theopen_participants/{participantId} {
      allow read: if true;
      allow create, update, delete: if isAdmin();
    }

    // Greedy Participants: Anyone can read, only admin can write
    match /theopen_greedyParticipants/{participantId} {
      allow read: if true;
      allow create, update, delete: if isAdmin();
    }

    // PlayerScores: Anyone can read, only admin can write
    match /theopen_playerScores/{playerName} {
      allow read: if true;
      allow write: if isAdmin();
    }

    // PlayoffScores: Anyone can read, only admin can write
    match /theopen_playoffScores/{playerName} {
      allow read: if true;
      allow write: if isAdmin();
    }

    // User profiles (for RBAC)
    match /theopen_users/{userId} {
      allow read: if isAuthenticated() && request.auth.uid == userId;
      allow write: if isAdmin();
    }
  }
}
